<Page
    x:Class="MoneyTalk.App.Views.Pages.ItemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dg="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:controls="using:MoneyTalk.App.Controls">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Products &amp; Services" Style="{StaticResource PageTitleStyle}" Margin="0" />
            <Button Grid.Column="1" Content="Export CSV" Margin="0,0,8,0" Click="ExportCsv_Click" VerticalAlignment="Center" />
            <Button Grid.Column="2" Content="Add Item" Style="{StaticResource AccentButtonStyle}" Click="AddItem_Click" VerticalAlignment="Center" />
        </Grid>

        <controls:BusyErrorBar Grid.Row="1" IsBusy="{Binding IsBusy}" ErrorMessage="{Binding ErrorMessage}" />

        <Grid Grid.Row="2">
            <dg:DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False"
                         IsReadOnly="True" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                         GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                         DoubleTapped="ItemsGrid_DoubleTapped">
                <dg:DataGrid.Columns>
                    <dg:DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="100" />
                    <dg:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="220" />
                    <dg:DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120" />
                    <dg:DataGridTextColumn Header="Sales Price" Binding="{Binding SalesPrice}" Width="120" />
                    <dg:DataGridTextColumn Header="Cost" Binding="{Binding Cost}" Width="120" />
                    <dg:DataGridTemplateColumn Header="Qty on Hand" Width="150">
                        <dg:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="{Binding QuantityOnHand}" VerticalAlignment="Center" />
                                    <Border Background="{StaticResource MoneyTalkNegativeBrush}" CornerRadius="4" Padding="4,1"
                                            Visibility="{Binding Converter={StaticResource LowStockToVisibilityConverter}}">
                                        <TextBlock Text="Low stock" Foreground="White" FontSize="11" />
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </dg:DataGridTemplateColumn.CellTemplate>
                    </dg:DataGridTemplateColumn>
                    <dg:DataGridTemplateColumn Header="" Width="110">
                        <dg:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Print Label" Click="PrintLabel_Click" Tag="{Binding}" />
                            </DataTemplate>
                        </dg:DataGridTemplateColumn.CellTemplate>
                    </dg:DataGridTemplateColumn>
                </dg:DataGrid.Columns>
            </dg:DataGrid>
            <TextBlock Text="No items yet. Click &quot;Add Item&quot; to create one." Style="{StaticResource MutedTextStyle}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding Items.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}" />
        </Grid>
    </Grid>
</Page>
