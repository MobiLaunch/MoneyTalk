<Page
    x:Class="MoneyTalk.App.Views.Pages.CalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dg="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:controls="using:MoneyTalk.App.Controls">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Calendar" Style="{StaticResource PageTitleStyle}" Margin="0" />
            <Button Grid.Column="1" Content="Add Appointment" Margin="0,0,8,0" Click="AddAppointment_Click" VerticalAlignment="Center" />
            <Button Grid.Column="2" Content="Add House Call" Style="{StaticResource AccentButtonStyle}" Click="AddHouseCall_Click" VerticalAlignment="Center" />
        </Grid>

        <controls:BusyErrorBar Grid.Row="1" IsBusy="{Binding IsBusy}" ErrorMessage="{Binding ErrorMessage}" />

        <Grid Grid.Row="2">
            <dg:DataGrid ItemsSource="{Binding Events}" AutoGenerateColumns="False"
                         IsReadOnly="True" SelectedItem="{Binding SelectedEvent, Mode=TwoWay}"
                         GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                         DoubleTapped="EventsGrid_DoubleTapped">
                <dg:DataGrid.Columns>
                    <dg:DataGridTextColumn Header="Type" Binding="{Binding EventType}" Width="110" />
                    <dg:DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120" />
                    <dg:DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="90" />
                    <dg:DataGridTextColumn Header="Title / Description" Binding="{Binding Title}" Width="2*" />
                    <dg:DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="180" />
                    <dg:DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
                </dg:DataGrid.Columns>
            </dg:DataGrid>
            <TextBlock Text="Nothing scheduled. Add an appointment or house call to get started." Style="{StaticResource MutedTextStyle}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding Events.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}" />
        </Grid>
    </Grid>
</Page>
